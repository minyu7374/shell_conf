if [[ "$1" = "force" ]] || [ -z "$SSH_AGENT_PID" ] || [ "$SSH_AGENT_PID" != "$(pgrep ssh-agent)" ]; then
    pkill -9 ssh-agent
    eval "$(ssh-agent)"

    echo "env SSH_AGENT_PID=$SSH_AGENT_PID"
    echo "env SSH_AUTH_SOCK=$SSH_AUTH_SOCK"

    ssh-add  "$HOME/.ssh/work/id_ed25519"
fi
